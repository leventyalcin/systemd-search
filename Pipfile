[[source]]
url = "https://pypi.org/simple"
verify_ssl = true
name = "pypi"

[packages]
# No runtime dependencies. Install the package for local use with: pip install -e .

[dev-packages]
pytest = ">=8.0"
build = ">=1.0"
molecule = ">=6.0"
molecule-plugins = {extras = ["docker"], version = ">=23.5"}
ansible-core = ">=2.15,<2.16"

[requires]
python_version = "3.9"
